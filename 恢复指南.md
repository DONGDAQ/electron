# 报价工具 - 新电脑恢复指南

> 目的：电脑坏了 / 换电脑时，5 分钟在新电脑跑起报价工具。
> 前提：**报价/结算数据不重要**（已确认），重点是工具本身能用。

---

## 一、需要的东西

| 东西 | 在哪 | 说明 |
|---|---|---|
| 代码 + 模板 | GitHub `DONGDAQ/electron` | 已全部入库（含启动器、vbs、模板） |
| 凭证 `credentials.json` | 备份在别处（见下文） | 飞书 secret + SMB 密码，**不在 git 里** |
| 开发版 exe | 随时可重新打包 | `报价工具(开发版).exe` |

---

## 二、新电脑步骤（一次性，约 10 分钟）

### 1. 装基础环境
- **Python 3.12**：到 python.org 下载安装，勾选 "Add to PATH"
- **Node.js 18+**：到 nodejs.org 下载安装（LTS 版即可）

### 2. 拉代码
```bash
git clone https://github.com/DONGDAQ/electron.git
cd electron
```

### 3. 装 Python 依赖
```bash
cd app
C:/Users/<你的用户名>/AppData/Local/Programs/Python/Python312/python.exe -m pip install -r ../requirements.txt
```
> 如果 pip 装不动，就安装依赖：`pip install flask openpyxl pywin32 python-docx requests`

### 4. 装 Electron 依赖
```bash
cd D:\baojia\electron   # 回到项目根
npm install
```
> 或者跳过：直接双击打包好的 `报价工具(开发版).exe`（它不依赖 node_modules 就能启动 Electron？——不，开发版 exe 只是启动器，还是需要 node_modules）
> ✅ **所以 node_modules 必须装**：`npm install` 一次即可

### 5. 恢复凭证
把备份的 `credentials.json` 放到：
```
D:\baojia\electron\app\config\credentials.json
```
内容格式（3 项都要有）：
```json
{
  "feishu": {
    "app_id": "cli_aa882f3b1abb5bb7",
    "app_secret": "<飞书密钥>"
  },
  "smb": {
    "user": "dong_daqian",
    "password": "<共享盘密码>"
  },
  "dingtalk": {
    "webhook": ""
  }
}
```

### 6. 启动
- 方式 A（推荐）：双击 `报价工具(开发版).exe`（带图标那个）
- 方式 B：`npm start` 或 `electron .`

---

## 三、备份凭证（现在就在旧电脑做一次）

把下面这个文件复制到**网盘 / 微信文件传输助手 / U盘**各存一份：
```
D:\baojia\electron\app\config\credentials.json
```
> 这是唯一的"钥匙"，丢了就得重新去飞书开放平台申请。

---

## 四、恢复每日自动报价（可选）

新电脑上跑一次：
```powershell
powershell -ExecutionPolicy Bypass -File setup_scheduled_task.ps1
```
（在项目根目录执行，注册"翻译报价每日自动报价"计划任务，工作日 9:00）

---

## 五、常见问题

**Q: 双击 exe 没反应？**
A: 等 10-20 秒（单文件解压），或看是否缺 node_modules——先 `npm install`。

**Q: 提示找不到 Python？**
A: 确认 Python 3.12 装在默认路径 `%LOCALAPPDATA%\Programs\Python\Python312\`，或改 `main.js` 里 `findPython()`。

**Q: 飞书报权限错误？**
A: credentials.json 没放对，或密钥填错。重新对照第二节第 5 步。

---

*最后更新：2026-08-04*
